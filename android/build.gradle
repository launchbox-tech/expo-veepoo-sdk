apply plugin: 'com.android.library'

group = 'expo.modules.veepoo'
version = '1.0.0'

def expoModulesCorePlugin = new File(project(":expo-modules-core").projectDir.absolutePath, "ExpoModulesCorePlugin.gradle")
apply from: expoModulesCorePlugin
applyKotlinExpoModulesCorePlugin()
useCoreDependencies()
useDefaultAndroidSdkVersions()

android {
  namespace "expo.modules.veepoo"

  defaultConfig {
    versionCode 1
    versionName "1.0.0"
  }

  lint {
    abortOnError false
  }
}

dependencies {
  // JVM-only tests live in src/test/kotlin/. AndroidJUnitRunner tests would
  // need androidTestImplementation and a real device/emulator — out of scope
  // for these surface checks, which only parse source files.
  testImplementation 'junit:junit:4.13.2'

  implementation "com.google.code.gson:gson:2.10.1"

  // AAR 子项目引用 - 使用正确的项目名称格式
  implementation project(':expo-veepoo-sdk$abpartool')
  implementation project(':expo-veepoo-sdk$bmpconvert')
  implementation project(':expo-veepoo-sdk$jl-bt-ota')
  implementation project(':expo-veepoo-sdk$jl-rcsp')
  implementation project(':expo-veepoo-sdk$jl-watch')
  implementation project(':expo-veepoo-sdk$libble')
  implementation project(':expo-veepoo-sdk$libdfu')
  implementation project(':expo-veepoo-sdk$libfastdfu')
  implementation project(':expo-veepoo-sdk$vpbluetooth')
  implementation project(':expo-veepoo-sdk$vpprotocol')

  // JAR files
  implementation fileTree(dir: "${project.projectDir}/libs", include: ['*.jar'])

  // React Native dependencies
  compileOnly "com.facebook.react:react-native:+"
}
