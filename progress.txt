## 2026-05-01 — Issue #8: real-time health tests (HR, BP, SpO2)

**Done:**
- Three HealthTestCard components in session screen: Heart Rate, Blood Pressure, SpO2
- Each card: Start button (disabled while another test is running), progress bar 0-100%,
  result display when state === 'over'; "Please wear the device" / "Test error" for edge states
- `heartRateTestResult`, `bloodPressureTestResult`, `bloodOxygenTestResult` listeners
  active via useEffect gated on appState === 'ready'
- `activeTest` state prevents two tests running concurrently
- Results cleared on new test start and on disconnect
- HR result: "X bpm" | BP result: "sys/dia mmHg · pulse bpm" | SpO2: "X% SpO₂"
- tsc --noEmit example: 0 errors ✅ | root tests: 6/6 pass ✅

**Note for next agent (Issue #9 — historical data sync + Issue #10 — device info):**
- Session screen has a placeholder comment `// Issues #9 and #10 go here`
- Issue #9: "Sync Data" button → `sdk.startReadOriginData()`, listen for
  `readOriginProgress` events (payload: { deviceId, progress: ReadOriginProgress }),
  `readOriginComplete`, `sleepData`, `sportStepData` events; show progress + summary
- Issue #10: call `sdk.readBattery()` and `sdk.readDeviceVersion()` on deviceReady
  to populate battery % and firmware version in the session header card
- Both can be done in a single pass — add them as cards above the Disconnect button

## 2026-05-01 — Issue #7: connect + session + disconnect/reconnect

**Done:**
- DeviceRow "Connect" → `stopScan()` then `sdk.connect(device.id)`, appState → 'connecting'
  Shows full-screen spinner with device name while pairing
- `deviceReady` listener (active in 'connecting' | 'ready') → appState → 'ready',
  calls `sdk.syncPersonalInfo(DEFAULT_PERSONAL_INFO)`; sync status shown in session card
- `deviceDisconnected` listener → appState → 'disconnected'; shows device name + Reconnect btn
- Reconnect button → re-enters scan flow via `handleStartScan()`
- Session screen ('ready'): device name in header, "Personal Info Sync" card with ✓/spinner,
  Disconnect button (red); placeholder comment for Issues #8, #9, #10
- Disconnect button → `sdk.disconnect()` (no-arg — SDK uses internal connectedDeviceId),
  appState → 'idle'
- DEFAULT_PERSONAL_INFO defined at module level (sex:1, h:175, w:70, age:30,
  stepAim:8000, sleepAim:480) — can be overridden by companion app
- tsc --noEmit example: 0 errors ✅ | root tests: 6/6 pass ✅

**Note for next agent (Issue #8 — HR/BP/SpO2 health tests):**
- The session screen ('ready' appState) has a placeholder comment in index.tsx for
  health test section — add below the Personal Info Sync card
- SDK methods: `sdk.startHeartRateTest()`, listen for `heartRateTestResult` events
  payload: `{ deviceId, result: HeartRateTestResult }` — check types.ts for TestState
- Similarly for BP: `startBloodPressureTest()` / `bloodPressureTestResult`
- And SpO2: `startBloodOxygenTest()` / `bloodOxygenTestResult`
- Each test has a `state` field (TestState) and a result value; show progress while
  state is 'testing', show result when state is 'success'
- Also check Issue #9 (data sync) and #10 (battery/device info) — both are ready to
  implement after #8

## 2026-05-01 — Issue #6: scan + device list

**Done:**
- "Start Scan" Pressable calls `sdk.startScan()`, clears device list, transitions to 'scanning'
- `deviceFound` listener registered in a useEffect gated on `appState === 'scanning'`;
  deduplicates by `device.id`, updates RSSI on repeat sightings of the same device
- FlatList renders `DeviceRow` per device: name, RSSI dBm, MAC, "Connect" Pressable
  (Connect stubbed — Issue #7)
- Empty-list placeholder: "Scanning for nearby HBand devices…"
- "Stop Scan" Pressable calls `sdk.stopScan()`, transitions back to 'idle'; list persists
  so user can still connect after stopping
- `deviceFound` listener cleaned up on effect teardown (state changes or unmount)
- tsc --noEmit example: 0 errors ✅ | root tests: 6/6 pass ✅

**Note for next agent (Issue #7 — connect + session + disconnect):**
- DeviceRow's `onConnect` prop is stubbed in index.tsx with a comment
- Next: call `sdk.connect(device.id)`, set appState to 'connecting', show spinner
- Listen for `deviceReady` event → transition to 'ready', call `sdk.syncPersonalInfo()`
- Listen for `deviceDisconnected` event → transition to 'disconnected', show Reconnect btn
- Session section should only render when appState === 'ready'
- syncPersonalInfo() signature in VeepooSDK.ts — check what PersonalInfo fields are needed

## 2026-05-01 — Issue #4: init + permissions + idle-state UI

**Done:**
- Replaced template index.tsx with idle-state UI that:
  - Calls `sdk.init()` then `sdk.requestPermissions()` on mount
  - Shows ActivityIndicator + "Initializing SDK…" while `appState === 'initializing'`
  - Shows "HBand Connect" screen with "Start Scan" button (disabled when !permissions.granted)
  - Shows permission warning text when permissions not yet granted
- Updated `_layout.tsx` to wrap app in `SafeAreaProvider`, removed Stack header
- Exported `PermissionsResult` type from module's `src/index.ts` (was missing)
- Added `"expo-veepoo-sdk": ["../src/index.ts"]` path alias to example tsconfig.json
  (bun's file: copy excludes gitignored build/; alias resolves from source directly)
- AppState type (`initializing | idle | scanning | connecting | ready | disconnected`)
  defined in index.tsx for future issues to extend
- tsc --noEmit in example: 0 errors ✅ | root tests: 6/6 pass ✅

**Note for next agent (Issue #6 — scan + device list):**
- The "Start Scan" `onPress` handler is stubbed with a comment in index.tsx
- Next step: call `sdk.startScan()`, transition to 'scanning', listen for `deviceFound`
  events, collect VeepooDevice[] into state, render in a FlatList
- Use `sdk.on('deviceFound', handler)` or the event subscription API
- Check VeepooSDK.ts around line ~393 for startScan() signature
- The state machine type is already set up in index.tsx as `AppState`

## 2026-05-01 — Issue #3: Example app scaffold + config plugin

**Done:**
- Scaffolded `example/` with `npx create-expo-app@latest --template default@sdk-55`
  (Expo SDK 55, RN 0.83.6, expo-router, Reanimated 4, New Arch)
- Added `expo-veepoo-sdk: "file:.."` to example/package.json; bun install resolves cleanly
- Added `bundleIdentifier` (tech.launchbox.veepoo.example) and `package` to app.json
- Wired `"expo-veepoo-sdk"` into example/app.json plugins
- Fixed config plugin (src/plugin/index.ts) to inject NSLocationWhenInUseUsageDescription
  (was missing; required for BLE scanning on iOS)
- Built module (`./node_modules/.bin/tsc` → build/) — module must be built locally since
  build/ is gitignored; `prepare` script handles this on `npm install` from GitHub
- `npx expo prebuild --clean` succeeds; verified:
  - AndroidManifest: BLUETOOTH, BLUETOOTH_ADMIN, BLUETOOTH_CONNECT, BLUETOOTH_SCAN,
    ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION ✅
  - Info.plist: NSBluetoothAlwaysUsageDescription, NSBluetoothPeripheralUsageDescription,
    NSLocationWhenInUseUsageDescription ✅
- tsc --noEmit: 0 errors ✅ | npm test: 6/6 pass ✅

**Note for next agent (Issue #4 — init + permissions + idle-state UI):**
- The example app uses expo-router (file-based routing under example/src/app/)
- The template has a default home screen at example/src/app/(tabs)/index.tsx
- Replace that screen with the idle-state UI: call init() + requestPermissions() on mount,
  render a "Start Scan" button that is disabled until permissions are granted
- The module exports: init(), requestPermissions(), startScan(), stopScan(), connect(),
  disconnect(), syncPersonalInfo(), startHeartRateTest(), etc. — see src/VeepooSDK.ts
- SDK types are in src/types.ts; VeepooDevice, ConnectionState, etc.
- The example/android/ and example/ios/ directories from prebuild ARE generated;
  they are gitignored per .gitignore (example/android/, example/ios/) so run prebuild
  before running on device
