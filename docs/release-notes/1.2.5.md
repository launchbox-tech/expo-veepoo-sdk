# 1.2.5

发布日期：待发布

## 变更摘要

- 升级包版本到 `1.2.5`
- 同步 `package.json` / `module.json` / `ios/VeepooSDK.podspec` 版本号
- 更新文档入口，确保 README 与 release notes 指向最新版本

## 详细变更

- 版本号统一：
  - `@gaozh1024/expo-veepoo-sdk` → `1.2.5`
  - `module.json` → `1.2.5`
  - `ios/VeepooSDK.podspec` → `1.2.5`
- 文档更新：
  - 根目录 `README.md` 的“当前最新版本”更新为 `1.2.5`
  - `docs/README.md` 新增 `release-notes/1.2.5.md` 索引

## 升级影响

- 不影响 Android
- 不影响 iOS 原生能力
- 不改变 JS / TS 接口和返回结构

## 发布前检查

建议发布前执行：

- `npm run typecheck`
- `npm run build`
- `npm run prepublishOnly`
- `npm pack --dry-run`
