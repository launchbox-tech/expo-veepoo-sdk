# 1.2.4

发布日期：待发布

## 变更摘要

- 升级包版本到 `1.2.4`
- 整理文档入口，统一以根目录 `README.md` 作为使用说明主入口
- 补充 `1.2.4` 版本升级说明，保持 release notes 连续性

## 文档调整

本版本主要聚焦发布整理，不引入新的 TypeScript API 变更。

调整内容：

- `package.json` / `module.json` / `ios/VeepooSDK.podspec` 版本同步升级到 `1.2.4`
- 根目录 `README.md` 更新为指向最新版本说明
- `docs/README.md` 更新 release notes 索引
- 历史版本 `1.2.3` 的文档引用修正为当前可用说明入口

## 升级影响

- 不影响 Android
- 不影响 iOS 原生能力
- 不改变 JS / TS 接口和返回结构
- 适合集成方按版本号升级并同步查看最新说明

## 发布前检查

建议发布前执行：

- `npm run typecheck`
- `npm run build`
- `npm pack --dry-run`
