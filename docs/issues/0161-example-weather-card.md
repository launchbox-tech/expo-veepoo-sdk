# feat(example): WeatherCard — read, set, and push weather data

**Issue:** #161
**Status:** Closed
**Labels:** enhancement, ready-for-agent
**Parent:** #154

## What to build

New `WeatherCard` covering the three weather SDK methods.

## Acceptance criteria

- [ ] "Read settings" → `readWeatherSettings()`, displays result as JSON
- [ ] "Set settings" → `setWeatherSettings({temperatureUnit:'celsius'})`, shows operation status
- [ ] "Push weather" → `pushWeatherData(hardcoded)`, shows operation status
