# 61 — docs(adr): ADR 0002 vendor upstream tracking

## Parent

#59

## What to build

Add **`docs/adr/0002`** documenting **vendor upstream tracking**: why git **submodules are not used** for HBandSDK repos; **manifest-only** binaries; **warn-only** `vendor:check`; where **vendor vocabulary** lives vs **Band** / **Session** in AGENTS. Respect **ADR 0001** (Veepoo naming unchanged).

## Acceptance criteria

- [ ] ADR filed under `docs/adr/` with sequential number after 0001
- [ ] Decision rationale references Parent PRD #59 and upstream wiki URLs (Android / iOS)
- [ ] Links release discipline: binary changes imply manifest + release notes (parity matrix updates referenced when applicable)

## Blocked by

None — can start immediately.
